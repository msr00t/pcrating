.fill-in
  .container
    .row.reviews
      = render  partial: 'ratings/review',
          collection: @reviews,
          as: :review,
          locals: { centered: false,
            display_title: false,
            show_user: true,
            truncate_review: true }